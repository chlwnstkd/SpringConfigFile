server:
  port: 11001

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    beans:
      enabled: true
    info:
      enabled: true
  server:
    address: localhost
    port: 11002

poly: 'BigData Dev! Change!!!'